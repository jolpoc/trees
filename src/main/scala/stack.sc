import scala.collection.mutable.Stack
val myStack = Stack("cookie", "milk","angry dog")
myStack
val current = myStack.pop()
val numbers = Stack(0)
(0 to 9).foreach(numbers.push(_))
val stackRange = Stack.range(0,9)
numbers
numbers.pop()
numbers.pop()
numbers.pop()
numbers.size
numbers.slice(0,6)